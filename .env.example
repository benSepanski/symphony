# Linear API key — required for real-agent mode (omit in mock mode).
# Generate one at https://linear.app/settings/api
LINEAR_API_KEY=

# Optional: override the Claude CLI command (defaults to "claude").
# CLAUDE_CLI=claude
