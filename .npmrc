# Refuse to install package versions published in the last 3 days.
# Most maintainer-compromise incidents are detected and yanked within
# hours to a few days; this buys us that window. Requires pnpm >= 10.16.
minimumReleaseAge=4320
